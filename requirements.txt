# Frontend Dependencies
# Note: This is for Python backend if needed, frontend dependencies will be managed via package.json

# Supabase Python client for backend API (if needed)
supabase==2.3.0

# Flask for backend API (if needed for AI integration)
flask==2.3.0
flask-cors==4.0.0

# AI/ML Dependencies
openai==1.3.0  # For AI mood analysis (free tier available)
anthropic==0.7.0  # Alternative AI provider

# Data processing
pandas==2.1.0
numpy==1.25.0

# Environment management
python-dotenv==1.0.0

# Development tools
pytest==7.4.0
black==23.0.0
flake8==6.0.0
